# Payload CMS + Next.js environment variables
# Copy this file to .env and fill in your values.

# PostgreSQL connection string (Neon, Railway, etc.)
# Use file:./rfci-dev.db for local SQLite development
DATABASE_URI=postgresql://user:password@host/dbname?sslmode=require

# Random 32+ character string — generate with: openssl rand -base64 32
PAYLOAD_SECRET=your-secret-here

# Public URL of your deployment (no trailing slash)
NEXT_PUBLIC_SERVER_URL=https://your-app.vercel.app

# Google Gemini API key (optional, server-side only)
GEMINI_API_KEY=
