# Improvement Queue

## Done

- [x] Rebuild the homepage `Environments` section to match the brief's four-tile image grid with interactive hotspot tooltips.

## Next Up

- [ ] High: Restore local validation in fresh worktrees by documenting or automating dependency installation, since `npm run lint` and `npm run build` fail without `node_modules`.
- [ ] High: Replace remaining hotlinked remote imagery with local or CMS-managed assets to reduce third-party dependency and visual drift.
- [ ] Medium: Audit the README section inventory against the shipped homepage so intentional additions are documented and stale brief items are removed.
