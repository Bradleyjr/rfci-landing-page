# Improvement Queue

1. Make hover-driven homepage interactions accessible on touch and keyboard.
   The environments flip cards and mission hover states rely heavily on pointer hover and need focus/tap-friendly behavior.
2. Replace remaining stock and remote-hosted imagery with owned RFCI or CMS-managed assets.
   Several homepage sections still depend on Unsplash or external image URLs, which weakens brand consistency and content control.
3. Remove or repurpose the unused LinkedIn feed component.
   The homepage no longer uses it, and the current implementation contains fabricated post content that should not be reintroduced without a real data source.
