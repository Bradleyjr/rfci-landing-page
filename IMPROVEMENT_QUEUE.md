# Improvement Queue

1. Rework the homepage environments section to match the spec's 2x2 image grid with interactive dot tooltips instead of flip cards.
2. Add proper keyboard navigation and ARIA tab/accordion semantics to the standards section.
3. Replace remaining generic external image fallbacks with RFCI-owned media so the landing page stays on-brand even without CMS content.
